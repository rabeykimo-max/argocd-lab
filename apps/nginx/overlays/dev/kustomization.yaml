apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nginx-dev

resources:
  - ../../base

replicas:
  - name: nginx
    count: 1

images:
  - name: nginx
    newTag: "1.29.1"

labels:
  - pairs:
      environment: dev
